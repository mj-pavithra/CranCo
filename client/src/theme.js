import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#2060AD', // Replace with your desired custom color
    },
  },
});

export default theme;
